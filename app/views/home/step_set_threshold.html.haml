.jumbotron
	= render 'layouts/messages'
	%h1 1) Set a price tag
	%p.lead I commit to answer to anyone who pays...
	= form_tag "set_threshold" do
		.input-prepend.input-append
			%span.add-on $
			= text_field_tag :threshold, params[:threshold]
			%span.add-on .00
		= submit_tag "Continue", :class => "btn btn-large btn-success"