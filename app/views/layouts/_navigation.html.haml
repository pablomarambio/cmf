%ul.nav.nav-pills.pull-right
  %li{ :class => :active }
    = link_to "Home", root_path
  - if user_signed_in?
    %li
      = link_to 'Logout', destroy_user_session_path, :method=>'delete'
  - else
    %li
      = link_to 'Login', new_user_session_path
  - if user_signed_in?
    %li
      = link_to 'Profile', profile_path#edit_user_registration_path
    - if current_user.has_role? :admin
      %li
        = link_to 'Admin', users_path
  - else
    %li
      = link_to 'Sign up',"#"# new_user_auth_path
  - if Rails.env.development?
    %li
      = link_to "Public Profiles (Only Development)", users_profile_path

%h3.muted My threshold
