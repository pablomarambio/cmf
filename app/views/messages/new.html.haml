.container
  .row-fluid
    .well.box-forms
      %h3
        Write the message for 
        = "#{@user.username}"
      %p.lead 
        Being concise will improve the odds of interesting 
        = "#{@user.name}"
        %br
      = render 'form'
